# Secrets
.env

# Go
/api/pitlane
/api/coverage.out

# Node
node_modules/
frontend/dist/

# Embedded SPA build — populated only inside the Docker build
/api/internal/api/dist/*
!/api/internal/api/dist/.gitkeep

# OS
.DS_Store

ADR.md
AGENT_PLAN.md