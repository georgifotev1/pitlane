# Implementation status

The server-rendered MVP is implemented under `api/cmd/web`.

Verification gate:

```sh
make audit
make build
```

Future work should stay within the feature scope in `ADR.md`.
